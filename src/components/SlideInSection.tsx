import { motion } from "framer-motion"; import { useInView } from "react-intersection-observer"; import { ReactNode } from "react"; interface SlideInSectionProps { children: ReactNode; direction?: "left" | "right"; } export default function SlideInSection({ children, direction = "left" }: SlideInSectionProps) { const { ref, inView } = useInView({ triggerOnce: true, threshold: 0.2 }); const slideVariants = { hidden: { opacity: 0, x: direction === "left" ? -50 : 50 }, visible: { opacity: 1, x: 0, transition: { duration: 0.6, ease: "easeOut" } }, }; return ( <motion.div ref={ref} initial="hidden" animate={inView ? "visible" : "hidden"} variants={slideVariants}> {children} </motion.div> ); } 